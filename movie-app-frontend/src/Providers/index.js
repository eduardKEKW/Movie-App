import { useMovieState, useMovieDispatch, useMovie, MovieProvider } from './MovieProvider';
import { useGlobalState, useGlobalDispatch, useGlobal, GlobalProvider } from './GlobalProvider';

 //export const useMovieState;
 //export const useMovieDispatch;
 //export const useMovie;
 ///export const useGlobalState;
 //export const useGlobalDispatch;
 //export const useGlobal;


export {
    useMovieState,
    useMovieDispatch,
    useMovie,
    MovieProvider,
    useGlobalState,
    useGlobalDispatch,
    useGlobal,
    GlobalProvider,
};